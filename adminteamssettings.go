// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

package sdk

import "context"

// AdminTeamsSettingsInfo retrieves settings information for a specified workspace. Provide `team_id` to identify the workspace whose settings you want to inspect. Authenticate with a token that has the `admin.teams:read` scope.
//
// # Fetch information about settings in a workspace
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:read`
func AdminTeamsSettingsInfo(ctx context.Context, clientConfig ClientConfig, teamId string, token string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "GET", "/admin.teams.settings.info", &RequestOpts{
		Query: map[string]any{
			"team_id": QueryValue{Value: teamId, Style: "form", Explode: true},
		},
		Headers: OperationHeaders(map[string]any{
			"token": token,
		}),
		OperationID: "AdminTeamsSettingsInfo",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}

// AdminTeamsSettingsSetDefaultChannels sets the default channels for a workspace. Supply `team_id` and `channel_ids` together with an authentication token that has the `admin.teams:write` scope. The `channel_ids` value identifies the channels that should be configured as defaults.
//
// Set the default channels of a workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	teamId: ID for the workspace to set the default channel for.
//	channelIds: An array of channel IDs.
func AdminTeamsSettingsSetDefaultChannels(ctx context.Context, clientConfig ClientConfig, token string, teamId string, channelIds string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "POST", "/admin.teams.settings.setDefaultChannels", &RequestOpts{
		Body: map[string]any{
			"token":       token,
			"team_id":     teamId,
			"channel_ids": channelIds,
		},
		ContentType: "application/x-www-form-urlencoded",
		OperationID: "AdminTeamsSettingsSetDefaultChannels",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}

// AdminTeamsSettingsSetDescription sets the description of a specified workspace. Provide `team_id` to select the workspace and `description` for its new description. Authenticate with a token that has the `admin.teams:write` scope.
//
// Set the description of a given workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	teamId: ID for the workspace to set the description for.
//	description: The new description for the workspace.
func AdminTeamsSettingsSetDescription(ctx context.Context, clientConfig ClientConfig, token string, teamId string, description string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "POST", "/admin.teams.settings.setDescription", &RequestOpts{
		Body: map[string]any{
			"team_id":     teamId,
			"description": description,
		},
		Headers: OperationHeaders(map[string]any{
			"token": token,
		}),
		ContentType: "application/x-www-form-urlencoded",
		OperationID: "AdminTeamsSettingsSetDescription",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}

// AdminTeamsSettingsSetDiscoverability sets the discoverability setting for a specified workspace. Supply `team_id` and choose the workspace's new `discoverability` value. The value must be `open`, `invite_only`, `closed`, or `unlisted`.
//
// # An API method that allows admins to set the discoverability of a given workspace
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	teamId: The ID of the workspace to set discoverability on.
//	discoverability: This workspace's discovery setting. It must be set to one
//	of `open`, `invite_only`, `closed`, or `unlisted`.
func AdminTeamsSettingsSetDiscoverability(ctx context.Context, clientConfig ClientConfig, token string, teamId string, discoverability string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "POST", "/admin.teams.settings.setDiscoverability", &RequestOpts{
		Body: map[string]any{
			"team_id":         teamId,
			"discoverability": discoverability,
		},
		Headers: OperationHeaders(map[string]any{
			"token": token,
		}),
		ContentType: "application/x-www-form-urlencoded",
		OperationID: "AdminTeamsSettingsSetDiscoverability",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}

// AdminTeamsSettingsSetIcon sets the icon for a workspace. Provide the workspace identifier in `team_id` and the icon image location in `image_url`. Authenticate the request with a token that has the `admin.teams:write` scope.
//
// Sets the icon of a workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	imageUrl: Image URL for the icon
//	teamId: ID for the workspace to set the icon for.
func AdminTeamsSettingsSetIcon(ctx context.Context, clientConfig ClientConfig, token string, imageUrl string, teamId string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "POST", "/admin.teams.settings.setIcon", &RequestOpts{
		Body: map[string]any{
			"token":     token,
			"image_url": imageUrl,
			"team_id":   teamId,
		},
		ContentType: "application/x-www-form-urlencoded",
		OperationID: "AdminTeamsSettingsSetIcon",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}

// AdminTeamsSettingsSetName sets the name of a workspace. Provide `team_id` and the new value in `name`, and authenticate with a token that has permission to modify workspace settings. The operation returns a brief success response when the name is updated.
//
// Set the name of a given workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	teamId: ID for the workspace to set the name for.
//	name: The new name of the workspace.
func AdminTeamsSettingsSetName(ctx context.Context, clientConfig ClientConfig, token string, teamId string, name string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "POST", "/admin.teams.settings.setName", &RequestOpts{
		Body: map[string]any{
			"team_id": teamId,
			"name":    name,
		},
		Headers: OperationHeaders(map[string]any{
			"token": token,
		}),
		ContentType: "application/x-www-form-urlencoded",
		OperationID: "AdminTeamsSettingsSetName",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}
