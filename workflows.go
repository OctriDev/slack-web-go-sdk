// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

package sdk

import "context"

// WorkflowsStepCompleted submits the successful completion of an app's workflow step execution. Provide `workflow_step_execute_id` to identify the execution and include `outputs` when the step produces configured output values.
//
// Indicate that an app's step in a workflow completed execution.
//
// Parameters:
//
//	workflowStepExecuteId: Context identifier that maps to the correct workflow
//	step execution.
//	token: Authentication token. Requires scope: `workflow.steps:execute`
//	outputs: Key-value object of outputs from your step. Keys of this object
//	reflect the configured `key` properties of your `outputs` array from your
//	`workflow_step` object.
func WorkflowsStepCompleted(ctx context.Context, clientConfig ClientConfig, workflowStepExecuteId string, token string, outputs *string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "GET", "/workflows.stepCompleted", &RequestOpts{
		Query: map[string]any{
			"workflow_step_execute_id": QueryValue{Value: workflowStepExecuteId, Style: "form", Explode: true},
			"outputs":                  QueryValue{Value: outputs, Style: "form", Explode: true},
		},
		Headers: OperationHeaders(map[string]any{
					"token": token,
		}),
		OperationID: "WorkflowsStepCompleted",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}

// WorkflowsStepFailed submits a failure notification for an app's workflow step execution. Identify the execution with `workflow_step_execute_id` and provide `error` as a JSON-based object containing a human-readable `message` property.
//
// Indicate that an app's step in a workflow failed to execute.
//
// Parameters:
//
//	workflowStepExecuteId: Context identifier that maps to the correct workflow
//	step execution.
//	error_: A JSON-based object with a `message` property that should contain a
//	human readable error message.
//	token: Authentication token. Requires scope: `workflow.steps:execute`
func WorkflowsStepFailed(ctx context.Context, clientConfig ClientConfig, workflowStepExecuteId string, error_ string, token string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "GET", "/workflows.stepFailed", &RequestOpts{
		Query: map[string]any{
			"workflow_step_execute_id": QueryValue{Value: workflowStepExecuteId, Style: "form", Explode: true},
			"error":                    QueryValue{Value: error_, Style: "form", Explode: true},
		},
		Headers: OperationHeaders(map[string]any{
					"token": token,
		}),
		OperationID: "WorkflowsStepFailed",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}

// WorkflowsUpdateStep updates the configuration for a workflow extension step. Use `workflow_step_edit_id` to identify the configuration context, then provide JSON-encoded `inputs` and `outputs` or override the displayed `step_name` and `step_image_url`. Embedded variables in `inputs` are defined and replaced by the workflow system, so you cannot create custom runtime variables.
//
// Update the configuration for a workflow extension step.
//
// Parameters:
//
//	workflowStepEditId: A context identifier provided with `view_submission`
//	payloads used to call back to `workflows.updateStep`.
//	token: Authentication token. Requires scope: `workflow.steps:execute`
//	inputs: A JSON key-value map of inputs required from a user during
//	configuration. This is the data your app expects to receive when the
//	workflow step starts. **Please note**: the embedded variable format is set
//	and replaced by the workflow system. You cannot create custom variables that
//	will be replaced at runtime. Read more about variables in workflow steps
//	here.
//	outputs: An JSON array of output objects used during step execution. This is
//	the data your app agrees to provide when your workflow step was executed.
//	stepName: An optional field that can be used to override the step name that
//	is shown in the Workflow Builder.
//	stepImageUrl: An optional field that can be used to override app image that
//	is shown in the Workflow Builder.
func WorkflowsUpdateStep(ctx context.Context, clientConfig ClientConfig, workflowStepEditId string, token string, inputs *string, outputs *string, stepName *string, stepImageUrl *string) (map[string]interface{}, error) {
	resp, err := DoCfg[map[string]interface{}](ctx, &clientConfig, "GET", "/workflows.updateStep", &RequestOpts{
		Query: map[string]any{
			"workflow_step_edit_id": QueryValue{Value: workflowStepEditId, Style: "form", Explode: true},
			"inputs":                QueryValue{Value: inputs, Style: "form", Explode: true},
			"outputs":               QueryValue{Value: outputs, Style: "form", Explode: true},
			"step_name":             QueryValue{Value: stepName, Style: "form", Explode: true},
			"step_image_url":        QueryValue{Value: stepImageUrl, Style: "form", Explode: true},
		},
		Headers: OperationHeaders(map[string]any{
					"token": token,
		}),
		OperationID: "WorkflowsUpdateStep",
	})
	if err != nil {
		return nil, err
	}
	return resp.Data, nil
}
