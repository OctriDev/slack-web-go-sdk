// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

package sdk

import "context"

type AdminAppsNamespace struct {
	config ClientConfig
}

func NewAdminAppsNamespace(config ClientConfig) *AdminAppsNamespace {
	return &AdminAppsNamespace{
		config: config,
	}
}

// Approve approves an app for installation on a workspace. Supply the authentication `token` with the `admin.apps:write` scope and identify the app or approval request using `app_id` or `request_id`.
//
// Approve an app for installation on a workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.apps:write`
//	appId: The id of the app to approve.
//	requestId: The id of the request to approve.
func (sdk *AdminAppsNamespace) Approve(ctx context.Context, token string, appId *string, requestId *string, teamId *string) (map[string]interface{}, error) {
	return AdminAppsApprove(ctx, sdk.config, token, appId, requestId, teamId)
}

// Restrict restricts an app from installation on a workspace. Supply the authentication `token` with the `admin.apps:write` scope and identify the app or request to restrict using `app_id` or `request_id`.
//
// Restrict an app for installation on a workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.apps:write`
//	appId: The id of the app to restrict.
//	requestId: The id of the request to restrict.
func (sdk *AdminAppsNamespace) Restrict(ctx context.Context, token string, appId *string, requestId *string, teamId *string) (map[string]interface{}, error) {
	return AdminAppsRestrict(ctx, sdk.config, token, appId, requestId, teamId)
}

type AdminAppsApprovedNamespace struct {
	config ClientConfig
}

func NewAdminAppsApprovedNamespace(config ClientConfig) *AdminAppsApprovedNamespace {
	return &AdminAppsApprovedNamespace{
		config: config,
	}
}

// List lists apps approved for an organization or workspace. Use `limit` to control the page size and pass the returned `next_cursor` in `cursor` to retrieve the next page.
//
// List approved apps for an org or workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.apps:read`
//	limit: The maximum number of items to return. Must be between 1 - 1000 both
//	inclusive.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page
func (sdk *AdminAppsApprovedNamespace) List(ctx context.Context, token string, limit *int64, cursor *string, teamId *string, enterpriseId *string) (map[string]interface{}, error) {
	return AdminAppsApprovedList(ctx, sdk.config, token, limit, cursor, teamId, enterpriseId)
}

type AdminAppsRequestsNamespace struct {
	config ClientConfig
}

func NewAdminAppsRequestsNamespace(config ClientConfig) *AdminAppsRequestsNamespace {
	return &AdminAppsRequestsNamespace{
		config: config,
	}
}

// List lists app installation requests for a team or workspace. Use `limit` to control the page size and pass the returned `next_cursor` in `cursor` to retrieve subsequent requests.
//
// List app requests for a team/workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.apps:read`
//	limit: The maximum number of items to return. Must be between 1 - 1000 both
//	inclusive.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page
func (sdk *AdminAppsRequestsNamespace) List(ctx context.Context, token string, limit *int64, cursor *string, teamId *string) (map[string]interface{}, error) {
	return AdminAppsRequestsList(ctx, sdk.config, token, limit, cursor, teamId)
}

type AdminAppsRestrictedNamespace struct {
	config ClientConfig
}

func NewAdminAppsRestrictedNamespace(config ClientConfig) *AdminAppsRestrictedNamespace {
	return &AdminAppsRestrictedNamespace{
		config: config,
	}
}

// List lists apps restricted from installation for an organization or workspace. Use `limit` to control the page size and pass the returned `next_cursor` in `cursor` to retrieve the next page.
//
// List restricted apps for an org or workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.apps:read`
//	limit: The maximum number of items to return. Must be between 1 - 1000 both
//	inclusive.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page
func (sdk *AdminAppsRestrictedNamespace) List(ctx context.Context, token string, limit *int64, cursor *string, teamId *string, enterpriseId *string) (map[string]interface{}, error) {
	return AdminAppsRestrictedList(ctx, sdk.config, token, limit, cursor, teamId, enterpriseId)
}

type AdminConversationsNamespace struct {
	config ClientConfig
}

func NewAdminConversationsNamespace(config ClientConfig) *AdminConversationsNamespace {
	return &AdminConversationsNamespace{
		config: config,
	}
}

// Archive archives a public or private channel. Supply `channel_id` in the form body and authenticate with a `token` that has the `admin.conversations:write` scope.
//
// Archive a public or private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The channel to archive.
func (sdk *AdminConversationsNamespace) Archive(ctx context.Context, token string, channelId string) (*AdminConversationsArchiveResponse, error) {
	return AdminConversationsArchive(ctx, sdk.config, token, channelId)
}

// ConvertToPrivate converts a public channel to a private channel. Supply `channel_id` in the form body and authenticate with a `token` that has the `admin.conversations:write` scope.
//
// Convert a public channel to a private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The channel to convert to private.
func (sdk *AdminConversationsNamespace) ConvertToPrivate(ctx context.Context, token string, channelId string) (*AdminConversationsConvertToPrivateResponse, error) {
	return AdminConversationsConvertToPrivate(ctx, sdk.config, token, channelId)
}

// Create creates a public or private channel-based conversation. Supply `name` and `is_private`, and provide `team_id` unless `org_wide` is true; the response includes the new channel identifier.
//
// Create a public or private channel-based conversation.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	name: Name of the public or private channel to create.
//	isPrivate: When `true`, creates a private channel instead of a public
//	channel
//	description: Description of the public or private channel to create.
//	orgWide: When `true`, the channel will be available org-wide. Note: if the
//	channel is not `org_wide=true`, you must specify a `team_id` for this
//	channel
//	teamId: The workspace to create the channel in. Note: this argument is
//	required unless you set `org_wide=true`.
func (sdk *AdminConversationsNamespace) Create(ctx context.Context, token string, name string, isPrivate bool, description *string, orgWide *bool, teamId *string) (*AdminConversationsCreateResponse, error) {
	return AdminConversationsCreate(ctx, sdk.config, token, name, isPrivate, description, orgWide, teamId)
}

// Delete deletes a public or private channel. Supply `channel_id` in the form body and authenticate with a `token` that has the `admin.conversations:write` scope.
//
// Delete a public or private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The channel to delete.
func (sdk *AdminConversationsNamespace) Delete(ctx context.Context, token string, channelId string) (*AdminConversationsDeleteResponse, error) {
	return AdminConversationsDelete(ctx, sdk.config, token, channelId)
}

// DisconnectShared disconnects a connected channel from one or more workspaces. Supply `channel_id` and optionally use `leaving_team_ids` to identify the workspace to remove; only one workspace identifier can currently be specified.
//
// Disconnect a connected channel from one or more workspaces.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The channel to be disconnected from some workspaces.
//	leavingTeamIds: The team to be removed from the channel. Currently only a
//	single team id can be specified.
func (sdk *AdminConversationsNamespace) DisconnectShared(ctx context.Context, token string, channelId string, leavingTeamIds *string) (*AdminConversationsDisconnectSharedResponse, error) {
	return AdminConversationsDisconnectShared(ctx, sdk.config, token, channelId, leavingTeamIds)
}

// GetConversationPrefs retrieves posting preferences for a public or private channel. Supply `channel_id` to identify the channel whose thread and posting permissions you want to inspect. The authentication token must include the required administrative read scope.
//
// Get conversation preferences for a public or private channel.
//
// Parameters:
//
//	channelId: The channel to get preferences for.
//	token: Authentication token. Requires scope: `admin.conversations:read`
func (sdk *AdminConversationsNamespace) GetConversationPrefs(ctx context.Context, channelId string, token string) (*AdminConversationsGetConversationPrefsResponse, error) {
	return AdminConversationsGetConversationPrefs(ctx, sdk.config, channelId, token)
}

// GetTeams lists the workspaces within an Enterprise organization to which a public or private channel is connected. Supply `channel_id` to identify the channel, then use `cursor` to retrieve additional pages. Use `limit` to control the page size.
//
// Get all the workspaces a given public or private channel is connected to within this Enterprise org.
//
// Parameters:
//
//	channelId: The channel to determine connected workspaces within the
//	organization for.
//	token: Authentication token. Requires scope: `admin.conversations:read`
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page
//	limit: The maximum number of items to return. Must be between 1 - 1000 both
//	inclusive.
func (sdk *AdminConversationsNamespace) GetTeams(ctx context.Context, channelId string, token string, cursor *string, limit *int64) (*AdminConversationsGetTeamsResponse, error) {
	return AdminConversationsGetTeams(ctx, sdk.config, channelId, token, cursor, limit)
}

// Invite invites one or more users to a public or private channel. Supply `user_ids` and `channel_id` in the form-encoded request body to identify the invitees and destination. The authenticated user must use a token with the administrative write scope.
//
// Invite a user to a public or private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	userIds: The users to invite.
//	channelId: The channel that the users will be invited to.
func (sdk *AdminConversationsNamespace) Invite(ctx context.Context, token string, userIds string, channelId string) (*AdminConversationsInviteResponse, error) {
	return AdminConversationsInvite(ctx, sdk.config, token, userIds, channelId)
}

// Rename updates the name of a public or private channel. Supply `channel_id` and the new `name` in the form-encoded request body. The authenticated user must use a token with the administrative write scope.
//
// Rename a public or private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The channel to rename.
func (sdk *AdminConversationsNamespace) Rename(ctx context.Context, token string, channelId string, name string) (*AdminConversationsRenameResponse, error) {
	return AdminConversationsRename(ctx, sdk.config, token, channelId, name)
}

// Search searches public and private channels across an Enterprise organization. Use `query`, `team_ids`, and `search_channel_types` to narrow the search, and use `sort` with `sort_dir` to control result ordering. Pass `cursor` from the previous response to retrieve the next page.
//
// Search for public or private channels in an Enterprise organization.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:read`
//	teamIds: Comma separated string of team IDs, signifying the workspaces to
//	search through.
//	query: Name of the the channel to query by.
//	limit: Maximum number of items to be returned. Must be between 1 - 20 both
//	inclusive. Default is 10.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page.
//	searchChannelTypes: The type of channel to include or exclude in the search.
//	For example `private` will search private channels, while `private_exclude`
//	will exclude them. For a full list of types, check the Types section.
//	sort: Possible values are `relevant` (search ranking based on what we think
//	is closest), `name` (alphabetical), `member_count` (number of users in the
//	channel), and `created` (date channel was created). You can optionally pair
//	this with the `sort_dir` arg to change how it is sorted
//	sortDir: Sort direction. Possible values are `asc` for ascending order like
//	(1, 2, 3) or (a, b, c), and `desc` for descending order like (3, 2, 1) or
//	(c, b, a)
func (sdk *AdminConversationsNamespace) Search(ctx context.Context, token string, teamIds *string, query *string, limit *int64, cursor *string, searchChannelTypes *string, sort *string, sortDir *string) (*AdminConversationsSearchResponse, error) {
	return AdminConversationsSearch(ctx, sdk.config, token, teamIds, query, limit, cursor, searchChannelTypes, sort, sortDir)
}

// SearchPaginated searches public and private channels across an Enterprise organization. Use `query`, `team_ids`, and `search_channel_types` to narrow the search, and use `sort` with `sort_dir` to control result ordering. Pass `cursor` from the previous response to retrieve the next page.
//
// Search for public or private channels in an Enterprise organization.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:read`
//	teamIds: Comma separated string of team IDs, signifying the workspaces to
//	search through.
//	query: Name of the the channel to query by.
//	limit: Maximum number of items to be returned. Must be between 1 - 20 both
//	inclusive. Default is 10.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page.
//	searchChannelTypes: The type of channel to include or exclude in the search.
//	For example `private` will search private channels, while `private_exclude`
//	will exclude them. For a full list of types, check the Types section.
//	sort: Possible values are `relevant` (search ranking based on what we think
//	is closest), `name` (alphabetical), `member_count` (number of users in the
//	channel), and `created` (date channel was created). You can optionally pair
//	this with the `sort_dir` arg to change how it is sorted
//	sortDir: Sort direction. Possible values are `asc` for ascending order like
//	(1, 2, 3) or (a, b, c), and `desc` for descending order like (3, 2, 1) or
//	(c, b, a)
func (sdk *AdminConversationsNamespace) SearchPaginated(ctx context.Context, token string, teamIds *string, query *string, limit *int64, cursor *string, searchChannelTypes *string, sort *string, sortDir *string, yield func(*ObjsChannel) bool) error {
	return AdminConversationsSearchPaginated(ctx, sdk.config, token, teamIds, query, limit, cursor, searchChannelTypes, sort, sortDir, yield)
}

// SetConversationPrefs updates the posting permissions for a public or private channel. Supply `channel_id` and a stringified JSON `prefs` value describing the desired conversation preferences. The authenticated user must use a token with the administrative write scope.
//
// Set the posting permissions for a public or private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The channel to set the prefs for
//	prefs: The prefs for this channel in a stringified JSON format.
func (sdk *AdminConversationsNamespace) SetConversationPrefs(ctx context.Context, token string, channelId string, prefs string) (*AdminConversationsSetConversationPrefsResponse, error) {
	return AdminConversationsSetConversationPrefs(ctx, sdk.config, token, channelId, prefs)
}

// SetTeams sets the workspaces connected to a public or private channel in an Enterprise Grid organization. Supply `channel_id`, and use `team_id`, `target_team_ids`, or `org_channel` to control the channel's workspace sharing.
//
// Set the workspaces in an Enterprise grid org that connect to a public or private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The encoded `channel_id` to add or remove to workspaces.
//	teamId: The workspace to which the channel belongs. Omit this argument if
//	the channel is a cross-workspace shared channel.
//	targetTeamIds: A comma-separated list of workspaces to which the channel
//	should be shared. Not required if the channel is being shared org-wide.
//	orgChannel: True if channel has to be converted to an org channel
func (sdk *AdminConversationsNamespace) SetTeams(ctx context.Context, token string, channelId string, teamId *string, targetTeamIds *string, orgChannel *bool) (map[string]interface{}, error) {
	return AdminConversationsSetTeams(ctx, sdk.config, token, channelId, teamId, targetTeamIds, orgChannel)
}

// Unarchive unarchives a public or private channel. Supply the channel identifier in `channel_id` and use an authentication token with the required administrative scope.
//
// Unarchive a public or private channel.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	channelId: The channel to unarchive.
func (sdk *AdminConversationsNamespace) Unarchive(ctx context.Context, token string, channelId string) (*AdminConversationsUnarchiveResponse, error) {
	return AdminConversationsUnarchive(ctx, sdk.config, token, channelId)
}

type AdminConversationsEkmNamespace struct {
	config ClientConfig
}

func NewAdminConversationsEkmNamespace(config ClientConfig) *AdminConversationsEkmNamespace {
	return &AdminConversationsEkmNamespace{
		config: config,
	}
}

// ListOriginalConnectedChannelInfo lists disconnected channels and their original channel IDs for Enterprise Key Management key revocation. Use `channel_ids` or `team_ids` to filter the results, and use `cursor` to retrieve subsequent pages. Set `limit` to control the maximum number of items returned.
//
// List all disconnected channels—i.e., channels that were once connected to other workspaces and then disconnected—and the corresponding original channel IDs for key revocation with EKM.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:read`
//	channelIds: A comma-separated list of channels to filter to.
//	teamIds: A comma-separated list of the workspaces to which the channels you
//	would like returned belong.
//	limit: The maximum number of items to return. Must be between 1 - 1000 both
//	inclusive.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page.
func (sdk *AdminConversationsEkmNamespace) ListOriginalConnectedChannelInfo(ctx context.Context, token string, channelIds *string, teamIds *string, limit *int64, cursor *string) (map[string]interface{}, error) {
	return AdminConversationsEkmListOriginalConnectedChannelInfo(ctx, sdk.config, token, channelIds, teamIds, limit, cursor)
}

type AdminConversationsRestrictAccessNamespace struct {
	config ClientConfig
}

func NewAdminConversationsRestrictAccessNamespace(config ClientConfig) *AdminConversationsRestrictAccessNamespace {
	return &AdminConversationsRestrictAccessNamespace{
		config: config,
	}
}

// AddGroup adds an identity-provider group to the allowlist for a private channel. Supply `group_id` and `channel_id` in the form-encoded request body, and include `team_id` when the channel is tied to one workspace. The group controls which users can access the channel.
//
// # Add an allowlist of IDP groups for accessing a channel
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	groupId: The [IDP
//	Group](https://slack.com/help/articles/115001435788-Connect-identity-provide
//	r-groups-to-your-Enterprise-Grid-org) ID to be an allowlist for the private
//	channel.
//	channelId: The channel to link this group to.
//	teamId: The workspace where the channel exists. This argument is required
//	for channels only tied to one workspace, and optional for channels that are
//	shared across an organization.
func (sdk *AdminConversationsRestrictAccessNamespace) AddGroup(ctx context.Context, token string, groupId string, channelId string, teamId *string) (map[string]interface{}, error) {
	return AdminConversationsRestrictAccessAddGroup(ctx, sdk.config, token, groupId, channelId, teamId)
}

// ListGroups lists the identity-provider groups linked to a channel. Supply `channel_id` to identify the channel and include `team_id` when the channel exists only in one workspace. Use a token with the administrative read scope.
//
// # List all IDP Groups linked to a channel
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:read`
//	teamId: The workspace where the channel exists. This argument is required
//	for channels only tied to one workspace, and optional for channels that are
//	shared across an organization.
func (sdk *AdminConversationsRestrictAccessNamespace) ListGroups(ctx context.Context, token string, channelId string, teamId *string) (map[string]interface{}, error) {
	return AdminConversationsRestrictAccessListGroups(ctx, sdk.config, token, channelId, teamId)
}

// RemoveGroup removes an identity-provider group from a private channel's allowlist. Supply `team_id`, `group_id`, and `channel_id` in the form-encoded request body to identify the workspace, group, and channel. Use a token with the administrative write scope.
//
// # Remove a linked IDP group linked from a private channel
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.conversations:write`
//	teamId: The workspace where the channel exists. This argument is required
//	for channels only tied to one workspace, and optional for channels that are
//	shared across an organization.
//	groupId: The [IDP
//	Group](https://slack.com/help/articles/115001435788-Connect-identity-provide
//	r-groups-to-your-Enterprise-Grid-org) ID to remove from the private channel.
//	channelId: The channel to remove the linked group from.
func (sdk *AdminConversationsRestrictAccessNamespace) RemoveGroup(ctx context.Context, token string, teamId string, groupId string, channelId string) (map[string]interface{}, error) {
	return AdminConversationsRestrictAccessRemoveGroup(ctx, sdk.config, token, teamId, groupId, channelId)
}

type AdminEmojiNamespace struct {
	config ClientConfig
}

func NewAdminEmojiNamespace(config ClientConfig) *AdminEmojiNamespace {
	return &AdminEmojiNamespace{
		config: config,
	}
}

// Add adds a custom emoji to an Enterprise Grid organization. Provide the emoji `name` and an image `url` so the organization can use the new emoji.
//
// Add an emoji.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	name: The name of the emoji to be removed. Colons (`:myemoji:`) around the
//	value are not required, although they may be included.
//	url: The URL of a file to use as an image for the emoji. Square images under
//	128KB and with transparent backgrounds work best.
func (sdk *AdminEmojiNamespace) Add(ctx context.Context, token string, name string, url string) (map[string]interface{}, error) {
	return AdminEmojiAdd(ctx, sdk.config, token, name, url)
}

// AddAlias adds an alias for an existing custom emoji. Provide the emoji `name` and the `alias_for` value to associate the alias with its target emoji.
//
// Add an emoji alias.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	name: The name of the emoji to be aliased. Colons (`:myemoji:`) around the
//	value are not required, although they may be included.
//	aliasFor: The alias of the emoji.
func (sdk *AdminEmojiNamespace) AddAlias(ctx context.Context, token string, name string, aliasFor string) (map[string]interface{}, error) {
	return AdminEmojiAddAlias(ctx, sdk.config, token, name, aliasFor)
}

// List lists custom emoji for an Enterprise Grid organization. Use `cursor` to retrieve subsequent pages and `limit` to control the number of items returned in each page.
//
// List emoji for an Enterprise Grid organization.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:read`
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page
//	limit: The maximum number of items to return. Must be between 1 - 1000 both
//	inclusive.
func (sdk *AdminEmojiNamespace) List(ctx context.Context, token string, cursor *string, limit *int64) (map[string]interface{}, error) {
	return AdminEmojiList(ctx, sdk.config, token, cursor, limit)
}

// Remove removes a custom emoji across an Enterprise Grid organization. Provide the emoji `name` together with an authentication token that has the required administrative scope.
//
// # Remove an emoji across an Enterprise Grid organization
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	name: The name of the emoji to be removed. Colons (`:myemoji:`) around the
//	value are not required, although they may be included.
func (sdk *AdminEmojiNamespace) Remove(ctx context.Context, token string, name string) (map[string]interface{}, error) {
	return AdminEmojiRemove(ctx, sdk.config, token, name)
}

// Rename renames a custom emoji in an Enterprise Grid organization. Provide the current emoji `name` and the replacement value in `new_name`.
//
// Rename an emoji.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	name: The name of the emoji to be renamed. Colons (`:myemoji:`) around the
//	value are not required, although they may be included.
//	newName: The new name of the emoji.
func (sdk *AdminEmojiNamespace) Rename(ctx context.Context, token string, name string, newName string) (map[string]interface{}, error) {
	return AdminEmojiRename(ctx, sdk.config, token, name, newName)
}

type AdminInviteRequestsNamespace struct {
	config ClientConfig
}

func NewAdminInviteRequestsNamespace(config ClientConfig) *AdminInviteRequestsNamespace {
	return &AdminInviteRequestsNamespace{
		config: config,
	}
}

// Approve approves a workspace invite request. Supply `invite_request_id` to identify the request, and use `team_id` when you need to specify the workspace where it was made.
//
// Approve a workspace invite request.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.invites:write`
//	inviteRequestId: ID of the request to invite.
//	teamId: ID for the workspace where the invite request was made.
func (sdk *AdminInviteRequestsNamespace) Approve(ctx context.Context, token string, inviteRequestId string, teamId *string) (map[string]interface{}, error) {
	return AdminInviteRequestsApprove(ctx, sdk.config, token, inviteRequestId, teamId)
}

// Deny denies a pending workspace invite request. Supply `invite_request_id` to identify the request and optionally provide `team_id` to identify its workspace. Authenticate with a token that has the `admin.invites:write` scope.
//
// Deny a workspace invite request.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.invites:write`
//	inviteRequestId: ID of the request to invite.
//	teamId: ID for the workspace where the invite request was made.
func (sdk *AdminInviteRequestsNamespace) Deny(ctx context.Context, token string, inviteRequestId string, teamId *string) (map[string]interface{}, error) {
	return AdminInviteRequestsDeny(ctx, sdk.config, token, inviteRequestId, teamId)
}

// List lists pending workspace invite requests. Use `team_id` to limit results to one workspace, `cursor` to continue from a previous page, and `limit` to control the number of results returned. Authenticate with a token that has the `admin.invites:read` scope.
//
// List all pending workspace invite requests.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.invites:read`
//	teamId: ID for the workspace where the invite requests were made.
//	cursor: Value of the `next_cursor` field sent as part of the previous API
//	response
//	limit: The number of results that will be returned by the API on each
//	invocation. Must be between 1 - 1000, both inclusive
func (sdk *AdminInviteRequestsNamespace) List(ctx context.Context, token string, teamId *string, cursor *string, limit *int64) (map[string]interface{}, error) {
	return AdminInviteRequestsList(ctx, sdk.config, token, teamId, cursor, limit)
}

type AdminInviteRequestsApprovedNamespace struct {
	config ClientConfig
}

func NewAdminInviteRequestsApprovedNamespace(config ClientConfig) *AdminInviteRequestsApprovedNamespace {
	return &AdminInviteRequestsApprovedNamespace{
		config: config,
	}
}

// List lists approved workspace invite requests. Use `team_id` to limit results to a workspace, `cursor` to continue pagination, and `limit` to control the page size.
//
// List all approved workspace invite requests.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.invites:read`
//	teamId: ID for the workspace where the invite requests were made.
//	cursor: Value of the `next_cursor` field sent as part of the previous API
//	response
//	limit: The number of results that will be returned by the API on each
//	invocation. Must be between 1 - 1000, both inclusive
func (sdk *AdminInviteRequestsApprovedNamespace) List(ctx context.Context, token string, teamId *string, cursor *string, limit *int64) (map[string]interface{}, error) {
	return AdminInviteRequestsApprovedList(ctx, sdk.config, token, teamId, cursor, limit)
}

type AdminInviteRequestsDeniedNamespace struct {
	config ClientConfig
}

func NewAdminInviteRequestsDeniedNamespace(config ClientConfig) *AdminInviteRequestsDeniedNamespace {
	return &AdminInviteRequestsDeniedNamespace{
		config: config,
	}
}

// List lists denied workspace invite requests. Use `team_id` to limit results to a workspace, `cursor` to continue pagination, and `limit` to control the page size.
//
// List all denied workspace invite requests.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.invites:read`
//	teamId: ID for the workspace where the invite requests were made.
//	cursor: Value of the `next_cursor` field sent as part of the previous api
//	response
//	limit: The number of results that will be returned by the API on each
//	invocation. Must be between 1 - 1000 both inclusive
func (sdk *AdminInviteRequestsDeniedNamespace) List(ctx context.Context, token string, teamId *string, cursor *string, limit *int64) (map[string]interface{}, error) {
	return AdminInviteRequestsDeniedList(ctx, sdk.config, token, teamId, cursor, limit)
}

type AdminTeamsAdminsNamespace struct {
	config ClientConfig
}

func NewAdminTeamsAdminsNamespace(config ClientConfig) *AdminTeamsAdminsNamespace {
	return &AdminTeamsAdminsNamespace{
		config: config,
	}
}

// List lists administrators for a specified workspace. Provide `team_id` to select the workspace, and use `cursor` and `limit` to control pagination and page size. Authenticate with a token that has the `admin.teams:read` scope.
//
// List all of the admins on a given workspace.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:read`
//	limit: The maximum number of items to return.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page.
func (sdk *AdminTeamsAdminsNamespace) List(ctx context.Context, token string, teamId string, limit *int64, cursor *string) (map[string]interface{}, error) {
	return AdminTeamsAdminsList(ctx, sdk.config, token, teamId, limit, cursor)
}

type AdminTeamsNamespace struct {
	config ClientConfig
}

func NewAdminTeamsNamespace(config ClientConfig) *AdminTeamsNamespace {
	return &AdminTeamsNamespace{
		config: config,
	}
}

// Create creates an Enterprise team. Supply `team_domain` and `team_name`, and optionally provide its description and discoverability setting. If provided, `team_discoverability` must be `open`, `closed`, `invite_only`, or `unlisted`.
//
// Create an Enterprise team.
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:write`
//	teamDomain: Team domain (for example, slacksoftballteam).
//	teamName: Team name (for example, Slack Softball Team).
//	teamDescription: Description for the team.
//	teamDiscoverability: Who can join the team. A team's discoverability can be
//	`open`, `closed`, `invite_only`, or `unlisted`.
func (sdk *AdminTeamsNamespace) Create(ctx context.Context, token string, teamDomain string, teamName string, teamDescription *string, teamDiscoverability *string) (map[string]interface{}, error) {
	return AdminTeamsCreate(ctx, sdk.config, token, teamDomain, teamName, teamDescription, teamDiscoverability)
}

// List lists all teams in an Enterprise organization. Use `cursor` to retrieve subsequent pages and `limit` to control the number of teams returned per request. Authenticate with a token that has the `admin.teams:read` scope.
//
// # List all teams on an Enterprise organization
//
// Parameters:
//
//	token: Authentication token. Requires scope: `admin.teams:read`
//	limit: The maximum number of items to return. Must be between 1 - 100 both
//	inclusive.
//	cursor: Set `cursor` to `next_cursor` returned by the previous call to list
//	items in the next page.
func (sdk *AdminTeamsNamespace) List(ctx context.Context, token string, limit *int64, cursor *string) (map[string]interface{}, error) {
	return AdminTeamsList(ctx, sdk.config, token, limit, cursor)
}

type AdminTeamsOwnersNamespace struct {
	config ClientConfig
}
